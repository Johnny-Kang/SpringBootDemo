server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssm_film?characterEncoding=utf8
    username: root
    password: root


#mybatis-plus配置控制台打印完整带参数SQL语句
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl